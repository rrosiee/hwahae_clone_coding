<%= form_with(model: rank_page, local: true) do |form| %>
  <% if rank_page.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(rank_page.errors.count, "error") %> prohibited this rank_page from being saved:</h2>

      <ul>
      <% rank_page.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
